// Job handler package. In 3D.2c-i this file only ensures the central
// registry is loaded before any consumer inspects it. Individual handlers
// land in 3D.2c-ii+.

export * from "../registry.server";